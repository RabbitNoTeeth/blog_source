{
  "name": "techblog",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "vuepress dev docs --temp .temp",
    "build": "export NODE_OPTIONS=--openssl-legacy-provider && vuepress build docs",
    "build-win": "set NODE_OPTIONS=--openssl-legacy-provider && vuepress build docs"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@vuepress/plugin-back-to-top": "^1.9.10",
    "@vuepress/plugin-nprogress": "^1.9.10",
    "@xiaopanda/vuepress-plugin-code-copy": "^1.0.3",
    "vuepress": "^1.9.10"
  }
}
